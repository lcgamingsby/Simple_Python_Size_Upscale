This code only increases the image resolution (without any increase in image quality).

To use it, you just need to run the upscale.py file.

If you experience an error with the package, you can immediately install the required package.
